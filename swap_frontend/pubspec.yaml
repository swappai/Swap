name: besmart_2025
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # ── Entra External ID auth (PKCE on web) ──────────────────────
  flutter_secure_storage: ^9.0.0
  crypto: ^3.0.3

  # ── Web platform ─────────────────────────────────────────────
  web: ^1.1.0

  # ── Existing packages ──────────────────────────────────────────
  model_viewer_plus: ^1.7.2
  image_picker: ^1.0.7
  http: ^1.5.0
  intl: ^0.20.2
  hugeicons: ^0.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/rainbow_emoji_blob.glb
    - assets/Hamburger.glb
    - assets/Robot.glb
    - assets/Planets.glb
    - assets/Swap-removebg-preview.png
    - assets/icon.glb
    

